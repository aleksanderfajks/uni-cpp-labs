#include <stdio.h>
#include "defs.h"

int main() {
	double result = avrage(indeksStud1, indeksStud2);
	printf("Addition result equals: %f \n", result);
	printf("Suma rozmiarow = %f", printSize());
	return 0;
}
